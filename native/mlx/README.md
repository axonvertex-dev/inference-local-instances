# Native MLX Runtime

This directory is intentionally not containerized. Use `../../scripts/start-mlx.sh` on an Apple Silicon Mac.

The virtual environment is created at:

```text
native/mlx/.venv
```

It is excluded by `.gitignore`.
